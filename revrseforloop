#include <stdio.h>

int main()
{
   int n;
   printf("enter the number \n");
   scanf("%d",&n);
   int reverse ;
   for(n!=0;reverse=n%10;n){
       printf("%d",reverse);
       n=n/10;
   }

    return 0;
}
